/**
 * @file MSOS-TestSuiteReport_20260912_c218f0f.js
 * @author Will Fobbs
 * @company Pooled Impact
 * @description FROZEN historical test-suite report, generated 2026-09-12T15:19:35.749Z.
 *   This is a real captured run, not a hand-typed summary -- see
 *   Docs/MSOS-Cleanup-Roadmap.md's "Last test run" section for the
 *   live/current version of this same discipline. This file never
 *   changes after being written; run it to reprint exactly what the
 *   suite printed on commit c218f0f.
 * @version 2026.09.12
 * Confidential & Proprietary — Pooled Impact. Not for distribution.
 */
(function(root, factory) {
  if (typeof module === 'object' && module.exports) module.exports = factory();
  else root.MSOSTestSuiteReport = factory();
}(typeof self !== 'undefined' ? self : this, function() {
  'use strict';
  const REPORT = "╔══════════════════════════════════════════════════════════════════════════════╗\n║ CONFIDENTIAL & PROPRIETARY — POOLED IMPACT                                   ║\n║ MountainShift OS — Full White-Box Test Suite Report                          ║\n║                                                                              ║\n║ Author:   Will Fobbs                                                         ║\n║ Company:  Pooled Impact (PooledImpact.com)                                   ║\n║ Version:  2026.09.12                                                         ║\n║ Date:     2026-09-12T15:19:35.749Z                                           ║\n║ Repo:     git.pooledimpact.com/Claude/Romans                                 ║\n║ Branch:   claude/devtools-overrides-robustness-8we96z                        ║\n║ Commit:   c218f0f9451559233b5f63dae3cba9b91730f682                           ║\n║                                                                              ║\n║ Not for distribution — Internal Use Only                                     ║\n╚══════════════════════════════════════════════════════════════════════════════╝\n\nSuites:\n  ✅ PASS  CPU.security.test.js  (15/15 checks)\n  ✅ PASS  Physical.security.test.js  (15/15 checks)\n  ✅ PASS  PreMixed.hazard.test.js  (9/9 checks)\n  ✅ PASS  StructureMixin.test.js  (23/23 checks)\n  ✅ PASS  Kernel.security.test.js  (15/15 checks)\n  ✅ PASS  BIOS.security.test.js  (12/12 checks)\n  ✅ PASS  FullBootChain.lifecycle.test.js  (16/16 checks)\n  ✅ PASS  NextInjection.audit.test.js  (9/9 checks)\n  ✅ PASS  Memory.security.test.js  (15/15 checks)\n  ✅ PASS  MemoryMapArena.test.js  (12/12 checks)\n  ✅ PASS  MemoryMapFS.test.js  (17/17 checks)\n  ✅ PASS  MemoryMapFS.nodeToNode.test.js  (7/7 checks)\n  ✅ PASS  BIOS.nvramFastPath.test.js  (8/8 checks)\n  ✅ PASS  ExtendX.stacking.test.js  (12/12 checks)\n  ✅ PASS  BIOS.firstBoot.test.js  (10/10 checks)\n\n────────────────────────────────────────────────────────────────────────────────\nFull captured output (node test/run-all.js file list, run individually\nas separate processes so module-level state cannot leak between files):\n────────────────────────────────────────────────────────────────────────────────\n\n=== CPU.security.test.js ===\nPASS: armed immediately after construction\nPASS: boot() succeeds while armed\nPASS: execute() succeeds while armed\nPASS: dumpState() succeeds while armed\nPASS: disarmed after dispose()\nPASS: execute() after dispose() is blocked, not falling through (threw: SecurityMixin: \"execute\" blocked on CPU -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: violation was recorded\nPASS: locked: disableLayer refuses the security mixin from construction, unsealed or not (threw: ExtendX.disableLayer(): mixin \"security:CPU\" is locked -- it cannot be toggled at runtime.)\nPASS: locked: enableLayer refuses the security mixin from construction, unsealed or not (threw: ExtendX.enableLayer(): mixin \"security:CPU\" is locked -- it cannot be toggled at runtime.)\nPASS: unsealed instance: dispose() still works normally pre-seal\nPASS: sealed instance: isSealed() true\nPASS: sealed instance: dispose is locked (threw: SecurityMixin: \"dispose\" is sealed on this instance. dispose/disposeAsync are locked once seal() has been called -- permanently, for this instance. A fresh, unsealed instance is the only way to get this capability back; there is no unseal().)\nPASS: sealed instance: disposeAsync is locked (threw: SecurityMixin: \"disposeAsync\" is sealed on this instance. dispose/disposeAsync are locked once seal() has been called -- permanently, for this instance. A fresh, unsealed instance is the only way to get this capability back; there is no unseal().)\nPASS: sealed instance: real, gated methods still work normally\nPASS: calling seal() again on an already-sealed instance is a safe no-op\n\nALL 15 CHECKS PASSED\n\n=== Physical.security.test.js ===\nPASS: _extId is reachable on a BaseClassX-Proxy-wrapped instance\nPASS: armed immediately after construction\nPASS: post() succeeds while armed\nPASS: getCPU() succeeds while armed\nPASS: DEFAULT (no cpuFactory injected): getCPU() returns a plain, unsecured CPU\nPASS: reset() succeeds while armed\nPASS: disarmed after dispose()\nPASS: post() after dispose() is blocked, not falling through (threw: SecurityMixin: \"post\" blocked on Physical -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: locked: disableLayer refuses the security mixin (unsealed instance, BaseClassX target) (threw: ExtendX.disableLayer(): mixin \"security:Physical\" is locked -- it cannot be toggled at runtime.)\nPASS: sealed\nPASS: sealed: dispose is locked (threw: SecurityMixin: \"dispose\" is sealed on this instance. dispose/disposeAsync are locked once seal() has been called -- permanently, for this instance. A fresh, unsealed instance is the only way to get this capability back; there is no unseal().)\nPASS: sealed: disableLayer is locked (threw: ExtendX.disableLayer(): mixin \"security:Physical\" is locked -- it cannot be toggled at runtime.)\nPASS: sealed: post()/getCPU() still work normally\nPASS: injected cpuFactory: getCPU() now returns a SECURED, armed CPU\nPASS: a Physical instance built WITHOUT cpuFactory is unaffected by the feature existing\n\nALL 15 CHECKS PASSED\n\n=== PreMixed.hazard.test.js ===\nPASS: separately composed: armed\nPASS: separately composed: execute() works, both layers run\nPASS: separately composed: after dispose(), execute() is STILL blocked -- security intact despite logging also composed (threw: SecurityMixin: \"execute\" blocked on CPU -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: locked survives the merge here (loggingMixin never touches that key -- a different second mixin could still clobber it)\nPASS: pre-mixed: armed\nPASS: HAZARD: execute() runs the LOGGING version only -- silently overwritten, no error\nPASS: but boot() -- untouched by the merge -- is still correctly gated post-dispose (threw: SecurityMixin: \"boot\" blocked on CPU -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: verify() on a genuine, untouched mixin: clean\nPASS: verify() on the pre-mixed hazard object: flags exactly \"execute\"\n\nALL 9 CHECKS PASSED\n\n=== StructureMixin.test.js ===\nPASS: root: no inferred parent (empty stack at construction)\nPASS: explicit addChild(): parent/child link established\nPASS: explicit removeChild(): link removed\nPASS: sync inference: constructing inside a dispatched method infers the caller as parent\nPASS: mode none: real method still works (transparent passthrough)\nPASS: mode none: no graph API is exposed\nPASS: dispose(): detaches from parent and orphans its own children\nPASS: relational: no graph API exposed (pure edges, no hierarchy)\nPASS: linkTo()/getConnected(): arbitrary edges, independent of any hierarchy\nPASS: unlinkFrom(): removes exactly the targeted edge\nPASS: ExtendX next()-collision hazard: unlinkFrom(target) with NO label arg still removes the edge (dispatcher injects a trailing next() callback into that slot -- label must never be compared against undefined)\nPASS: getConnected(): one hop only -- D does not see F or G directly\nPASS: getConnectedGraph(): D transitively reaches E, F, and G across 3 hops\nPASS: getConnectedGraph(): traversal is symmetric -- G (the far end) reaches back to D\nPASS: getConnectedGraph(): never includes the calling instance itself\nPASS: getConnectedGraph(): disconnected components stay disconnected\nPASS: getConnectedGraph(): unlinkFrom() severing a link shrinks the reachable set\nPASS: mode both: graph inference works\nPASS: mode both: relational edges also work, independent of the tree\nPASS: spawnChildren: all three children inferred parent = the spawning instance\nPASS: spawnChildren: parent.getChildren() lists all three, regardless of resolution order\nPASS: spawnChildren: each child sees the OTHER two as siblings, not itself\nPASS: documented limitation: a child constructed after its own factory awaits is NOT attributed to the spawning parent\n\nALL 23 CHECKS PASSED\n\n=== Kernel.security.test.js ===\nPASS: armed immediately after construction\nPASS: _host fix: attach() then getPhysical()/getEnvironment()/getMemory() see the SAME instance across separate dispatched calls\nPASS: next()-collision regression: fork(cmd) with NO ppid/memBytes still gets real numbers, not the injected next() callback\nPASS: fork(cmd, ppid) with explicit ppid but omitted memBytes: ppid is correct, memBytes still a real number\nPASS: next()-collision regression: tick() with NO quantum still runs a real quantum, not the injected next() callback\nPASS: tick(25): explicit quantum is honored\nPASS: kill() cascades to descendants and still works while armed\nPASS: disarmed after dispose()\nPASS: fork() after dispose() is blocked, not falling through (threw: SecurityMixin: \"fork\" blocked on Kernel -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: tick() after dispose() is blocked, not falling through (threw: SecurityMixin: \"tick\" blocked on Kernel -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: locked: disableLayer refuses the security mixin (threw: ExtendX.disableLayer(): mixin \"security:Kernel\" is locked -- it cannot be toggled at runtime.)\nPASS: sealed\nPASS: sealed: dispose is locked (threw: SecurityMixin: \"dispose\" is sealed on this instance. dispose/disposeAsync are locked once seal() has been called -- permanently, for this instance. A fresh, unsealed instance is the only way to get this capability back; there is no unseal().)\nPASS: sealed: fork()/tick() still work normally\nPASS: _host cleanup: a fresh instance reusing behavior is unaffected by a disposed one (Map entry actually removed, not leaked)\n\nALL 15 CHECKS PASSED\n\n=== BIOS.security.test.js ===\nPASS: armed immediately after construction\nPASS: injected kernelFactory: boot() returns a SECURED, armed Kernel\nPASS: default behavior unaffected: a BIOS built WITHOUT kernelFactory returns a plain Kernel\nPASS: next()-collision regression: boot(physical, fs) with NO iso does not crash even when Installer exists globally\nPASS: explicit addChild(): boot() correctly attributes the constructed Kernel as its child despite the async gap inference cannot cover\nPASS: BIOS composed WITHOUT StructureMixin: boot() does not crash on BaseClassX's own native addChild\nPASS: disarmed after dispose()\nPASS: boot() after dispose() is blocked, not falling through\nPASS: locked: disableLayer refuses the security mixin (threw: ExtendX.disableLayer(): mixin \"security:BIOS\" is locked -- it cannot be toggled at runtime.)\nPASS: sealed\nPASS: sealed: dispose is locked (threw: SecurityMixin: \"dispose\" is sealed on this instance. dispose/disposeAsync are locked once seal() has been called -- permanently, for this instance. A fresh, unsealed instance is the only way to get this capability back; there is no unseal().)\nPASS: sealed: boot() still works normally\n\nALL 12 CHECKS PASSED\n\n=== FullBootChain.lifecycle.test.js ===\nPASS: pre-boot: bios and physical are both armed independently\nPASS: post-boot: kernel is a SECURED, armed Kernel (BIOS.kernelFactory closed the raw-leak)\nPASS: post-boot: kernel.getPhysical() is the SAME secured physical instance passed to boot()\nPASS: post-boot: the CPU reachable through physical.getCPU() is SECURED and armed (cpuFactory closed the raw-leak)\nPASS: post-boot: structure graph -- bios is the (explicitly attributed) parent of kernel\nPASS: fork(): three processes created with correct ppid lineage (next()-collision fix holds under a real chain, not just in isolation)\nPASS: tick() x3: every process actually ran real CPU cycles through the secured Physical/CPU\nPASS: kill() cascades to editor and grep (both children of shell) under full-chain dispatch\nPASS: disposing bios does NOT cascade-dispose kernel -- kernel remains armed and functional\nPASS: disposing bios orphans kernel's structure link (detach, not re-parent)\nPASS: bios itself is correctly disarmed after its own dispose() (threw: SecurityMixin: \"boot\" blocked on BIOS -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: kernel disarmed after dispose(): fork() now blocked (threw: SecurityMixin: \"fork\" blocked on Kernel -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: cpu is still armed while physical (its owner) is not yet disposed\nPASS: physical disarmed after dispose(): post()/getCPU() now blocked (threw: SecurityMixin: \"getCPU\" blocked on Physical -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: disposing physical does NOT reach into the CPU it once held -- the CPU instance itself has its own independent lifecycle and is still armed\nPASS: cpu disarmed after its OWN explicit dispose(): execute() now blocked (threw: SecurityMixin: \"execute\" blocked on CPU -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\n\nALL 16 CHECKS PASSED\n\n=== NextInjection.audit.test.js ===\nPASS: Environment: no instance methods exist to wrap -- getOwnPropertyNames(prototype) is empty besides constructor\nPASS: Environment.detect() (static) is unaffected by composition -- never touches ExtendX at all\nPASS: ISO: composed instance methods take no arguments -- next() injection has nowhere to land\nPASS: Installer: install() is a static method -- untouched by ExtendX composition, which only wraps prototype dispatch\nPASS: BootDeviceScan: a plain object (no constructor) cannot even be passed to ExtendX.extend() -- Reflect.construct rejects it immediately\nPASS: FileFsBootAdapter: findBootEntry() has no optional trailing param -- an injected next() lands in an unused 3rd slot, harmlessly\nPASS: Registry: save()'s omitted options is ACCIDENTALLY safe -- both destructured fields (.backend/.key) fall back regardless of what garbage lands there\nPASS: Registry: set(key) with value OMITTED silently stores the injected next() callback instead of undefined -- the documented, currently-unreachable gap\nPASS: Registry: set(key, value) with BOTH arguments explicit is unaffected -- the documented workaround holds\n\nALL 9 CHECKS PASSED\n\n=== Memory.security.test.js ===\nPASS: armed immediately after construction\nPASS: _backing fix: attach() then write()/read() see the SAME instance across separate dispatched calls\nPASS: next()-collision regression: attach() with NO cpu still falls back to a real Uint8Array, not the injected next() callback treated as a cpu\nPASS: attach(cpu) with a REAL cpu-shaped object still uses its .memory buffer correctly\nPASS: next()-collision regression: alloc(pid, length) with NO label still gets a real string, not the injected next() callback\nPASS: alloc(pid, length, label) with an explicit label is honored\nPASS: free() removes the allocated region while armed\nPASS: disarmed after dispose()\nPASS: write() after dispose() is blocked, not falling through (threw: SecurityMixin: \"write\" blocked on Memory -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: alloc() after dispose() is blocked, not falling through (threw: SecurityMixin: \"alloc\" blocked on Memory -- no activation token. This instance was never armed (init() never ran), or dispose()/disposeAsync() already revoked it.)\nPASS: locked: disableLayer refuses the security mixin (threw: ExtendX.disableLayer(): mixin \"security:Memory\" is locked -- it cannot be toggled at runtime.)\nPASS: sealed\nPASS: sealed: dispose is locked (threw: SecurityMixin: \"dispose\" is sealed on this instance. dispose/disposeAsync are locked once seal() has been called -- permanently, for this instance. A fresh, unsealed instance is the only way to get this capability back; there is no unseal().)\nPASS: sealed: attach()/alloc()/read()/write() still work normally\nPASS: StructureMixin composes alongside SecurityMixin without conflict\n\nALL 15 CHECKS PASSED\n\n=== MemoryMapArena.test.js ===\nPASS: init(): maxSlots() reports a real, positive slot count\nPASS: writeSlot() before authenticate() is rejected (nonzero rc)\nPASS: authenticate() with the WRONG key is rejected\nPASS: authenticate() with the CORRECT key succeeds\nPASS: writeSlot()/readSlot() work once authenticated\nPASS: packBytes()/unpackBytes(): plain ASCII round-trips exactly\nPASS: packBytes()/unpackBytes(): multi-byte UTF-8 (emoji) round-trips exactly\nPASS: packBytes()/unpackBytes(): byte length not a multiple of 3 (the padding edge case) round-trips exactly\nPASS: Registry.saveToArena()/loadFromArena(): full round-trip preserves every entry exactly\nPASS: Registry.loadFromArena(): reading an untouched slot range falls back to a fresh default Registry, not a crash\nPASS: Registry.saveToArena() is blocked before authenticate() the same way raw writeSlot() is\nPASS: deauthenticate() actually revokes write access\n\nALL 12 CHECKS PASSED\n\n=== MemoryMapFS.test.js ===\nPASS: init(): mm_getBase()/mm_getSize() report real, positive values\nPASS: create(): returns a nonzero mailbox address\nPASS: create(): a real 32-byte access key is readable at the documented offset\nPASS: status() before authenticate() reports locked (0)\nPASS: auth() with a WRONG key is rejected\nPASS: auth() with the REAL access key succeeds\nPASS: status() after authenticate() reports unlocked (1)\nPASS: writeSlot()/readSlot() round-trip a float value\nPASS: writeSlot(): an inline blob over 20 bytes is rejected before ever touching WASM\nPASS: writeSlot(): an inline blob within the 20-byte limit round-trips via readMemory at the same scratch offset used to write it\nPASS: findFreeSlot()/markOccupied()/isOccupied()/markFree() behave consistently\nPASS: kvStore()/kvLookup(): stores and finds a key exactly\nPASS: kvLookup(): a missing key returns null, not a crash\nPASS: kvDelete(): removes the key so a subsequent kvLookup() returns null\nPASS: chainLink()/chainNext()/chainFind(): links two mailboxes and both directions resolve\nPASS: rotateKeys(): succeeds on an authenticated mailbox\nPASS: destroy(): succeeds once, and a second destroy() on the same address is rejected\n\nALL 17 CHECKS PASSED\n\n=== MemoryMapFS.nodeToNode.test.js ===\nPASS: setup: main thread create()/auth()/writeSlot() all succeed before handing off to the worker\nPASS: worker thread: a SECOND init() against the already-init'd shared memory does not error\nPASS: worker thread: sees the SAME base/size as main -- the shared arena, not a fresh one\nPASS: worker thread: reads the value main wrote, through its OWN independent WebAssembly.Instance\nPASS: worker thread: sees the mailbox as already authenticated -- auth state lives in shared arena data, not per-instance JS state\nPASS: worker thread: its own writeSlot()/readSlot() round-trip locally\nPASS: main thread: reads back what the WORKER wrote, after the worker exits -- real bidirectional sharing, not a one-way handoff\n\nALL 7 CHECKS PASSED\n\n=== BIOS.nvramFastPath.test.js ===\nPASS: no Registry attached: boot() still performs the full scan (esp before disk)\nPASS: first boot with an empty Registry: full scan happens (no confirmed entry to try yet)\nPASS: first boot: a CONFIRMED entry is written back to the Registry afterward\nPASS: second boot: fast path tries ONLY the confirmed device -- esp is never queried\nPASS: fast-path miss: tries the stale confirmed device first (disk), then falls through to the full scan\nPASS: fast-path miss: the Registry record is updated to the NEW confirmed device\nPASS: policy override: a confirmed device no longer in bootDeviceOrder is never queried by the fast path\nPASS: an unconfirmed raw BootDeviceScan hit is never persisted as a confirmed NVRAM entry\n\nALL 8 CHECKS PASSED\n\n=== ExtendX.stacking.test.js ===\nPASS: stacked composition: the INNER layer's method still dispatches\nPASS: stacked composition: the OUTER layer's method dispatches -- this is exactly what silently returned undefined before the fix\nPASS: stacked composition: instance instanceof Outer -- false before the fix\nPASS: stacked composition: instance instanceof Inner still holds\nPASS: stacked composition: the instance's DIRECT prototype is Outer.prototype, not Inner.prototype\nPASS: three layers deep: all three methods dispatch\nPASS: three layers deep: instanceof holds for every layer\nPASS: three layers deep: direct prototype is the OUTERMOST class\nPASS: non-nested composition: unaffected by the new.target fix\nPASS: stacked dispose(): BOTH the outer layer's and the inner layer's mixin hooks run\nPASS: stacked dispose(): a SECOND top-level dispose() call does not re-run either layer's hook\nPASS: stacked disposeAsync(): all three layers' mixin hooks run\n\nALL 12 CHECKS PASSED\n\n=== BIOS.firstBoot.test.js ===\nPASS: a fresh Registry defaults firstBootComplete to false and has no machineId yet\nPASS: first successful boot: firstBootComplete becomes true\nPASS: first successful boot: a machineId is minted\nPASS: steady-state boot: machineId is NOT regenerated on a later boot\nPASS: a failed boot (no bootable device found) does NOT mark firstBootComplete\nPASS: a failed boot does NOT mint a machineId\nPASS: a later successful boot, after an earlier failure, still runs first-boot setup\nPASS: no Registry attached: boot() completes without crashing (first-boot setup has nowhere to persist, and that is fine)\nPASS: a pre-existing machineId (from a partial prior run) is never overwritten\nPASS: firstBootComplete still gets marked true even when machineId already existed\n\nALL 10 CHECKS PASSED\n\n────────────────────────────────────────────────────────────────────────────────\n╔══════════════════════════════════════════════════════════════════════════════╗\n║ Report Complete                                                              ║\n║                                                                              ║\n║ Total: 195/195 checks passing, 15/15 suites green                            ║\n║ Overall result: ✅ ALL GREEN                                                  ║\n║                                                                              ║\n║ 2026-09-12T15:19:35.749Z                                                     ║\n║ Will Fobbs · Pooled Impact · Confidential & Proprietary                      ║\n╚══════════════════════════════════════════════════════════════════════════════╝\n";
  function run() { console.log(REPORT); return REPORT; }
  return { run: run, generatedAt: "2026-09-12T15:19:35.749Z", commit: "c218f0f9451559233b5f63dae3cba9b91730f682" };
}));
