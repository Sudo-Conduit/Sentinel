# journals/

A home for journal entries from this session and future sessions — kept
separate from `journey/journey.sqlite` (ASSAY's own journal, on its own
branch/PR) rather than merged into it.

Nothing else is decided yet: format, schema, and what goes here first are
open. This folder exists so the space is real before the content is,
same reasoning as `WAKEUP.md` and `journey.sqlite` existing before either
was full.
